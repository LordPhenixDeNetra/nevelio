config:
  init:
    welcome:    "Nevelio — Initial Setup"
    separator:  "────────────────────────"
    name:       "Your name"
    lang:       "Default language (fr/en/es)"
    ai_enable:  "Enable AI? (yes/no)"
    provider:   "AI provider"
    providers_list: "anthropic | openai | ollama | groq | mistral | bedrock"
    key_env:    "Environment variable for the API key"
    key_found:  "Key found in environment ✓"
    key_miss:   "Key not found — set it before using AI features"
    profile:    "Default scan profile (stealth/standard/aggressive)"
    done:       "Config written to %{path} ✓"
    already_exists: "A config file already exists: %{path}"
    overwrite:  "Overwrite? (yes/no)"
    skipped:    "Setup skipped."

  cmd:
    show_title:  "Resolved configuration"
    show_global: "Source: global config (%{path})"
    show_project:"Source: project config (nevelio.toml)"
    show_cli:    "Source: CLI flags"
    get_miss:    "Key '%{key}' not found in configuration"
    set_ok:      "'%{key}' updated → %{value}"
    set_err:     "Cannot update '%{key}': %{error}"
    reset_confirm: "Reset config to defaults? (yes/no)"
    reset_ok:    "Config reset ✓"
    reset_skip:  "Cancelled."
    edit_open:   "Opening %{path} in %{editor}..."
    edit_no_editor: "$EDITOR is not set. Edit manually: %{path}"
    no_global:   "No global config found. Run: nevelio config init"

  ai:
    ping_title:  "Testing configured AI providers"
    ping_ok:     "%{provider} ✓  (%{ms}ms) — model: %{model}"
    ping_fail:   "%{provider} ✗  — %{error}"
    ping_no_key: "%{provider} —  API key missing (%{env})"
    ping_local:  "%{provider} ✓  (local, %{ms}ms) — model: %{model}"
    no_providers:"No providers configured. Run: nevelio config init"
